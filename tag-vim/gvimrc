set guifont=Monospace\ 15

" Since we cannot enable underculr in terminal Vim, we replace it with
" underline. But we enable it for GVim:
let g:gruvbox_undercurl = 1
colorscheme gruvbox
set background=light
